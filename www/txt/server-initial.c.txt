#include <stdio.h>

int main() {
    // 1. Create a socket
    // 2. Bind socket to a port
    // 3. Wait for client to connect
    // 4. Send Hello, World
    printf("Server: Initial version\n");
    return 0;
}

/*
# This: server-initial.c
# Prev: hello-client-server-overview.md
# Next: server-with-socket.c

# Build & run:
gcc server-initial.c -o server && ./server
*/

